#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n, c,d;
	cin >> n >> c;
	vector<int>h;
	for (int i = 0; i < n; i++)
	{
		cin >> d;
		h.push_back(d);
	}
	int i = 1, j = 1;
	sort(h);
	return 0;
}